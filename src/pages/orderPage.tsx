import type {NextPage} from "next";
import {NextSeo} from "next-seo";
import {Fragment, useState} from "react";
import { useRouter } from 'next/router';

const ThankYou : NextPage= () => {
    return (<Fragment>
        <NextSeo title="Order Page"/>
    </Fragment>)
}

export default ThankYou;
